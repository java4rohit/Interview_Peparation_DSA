package InternShalaProject;

import java.io.FileInputStream;
import java.io.IOException;

public class EmployeeTimeRecord {
    public static void main(String[] args) {
     /*   String excelFilePath = "path/to/your/excel/file.xlsx";

        try (FileInputStream fis = new FileInputStream(excelFilePath);
             Workbook workbook = WorkbookFactory.create(fis)) {

            Sheet sheet = workbook.getSheetAt(0); // Assuming data is in the first sheet
            int numRows = sheet.getPhysicalNumberOfRows();

            for (int rowIndex = 1; rowIndex < numRows; rowIndex++) { // Assuming the first row is the header
                Row row = sheet.getRow(rowIndex);

                String name = row.getCell(0).getStringCellValue(); // Assuming name is in the first column
                String position = row.getCell(1).getStringCellValue(); // Assuming position is in the second column

                if (hasWorkedForSevenConsecutiveDays(row)) {
                    System.out.println("Employee Name: " + name + ", Position: " + position);
                }
            }

        } catch (IOException | EncryptedDocumentException ex) {
            ex.printStackTrace();
        }*/
    }
}
