package java8.Stream;

import java.util.Arrays;
import java.util.List;

public class ConvertStream {

    public static void main(String[] args) {

        int[] premetiveArray = {1,2,3,4,5};

        Integer[] objecArray = {1,2,3,4,5};

       // Arrays.stream(premetiveArray).forEach(System.out::println);

        List<String> names = Arrays.asList("rohit","rahul","ankita");

       // names.stream().map(a->a.toUpperCase()).forEach(System.out::println);
;   //what is palidrom

    }
}
